def kvardat(a, b, c):
    d = b * b - 4 * a * c
    if d < 0:
        return ()
    elif d == 0:
        x1 = -b / (2 * a)
        return (x1,)
    else:
        x1 = (-b - d ** 0.5)/(2 * a)
        x2 = (-b + d ** 0.5)/(2 * a)
        return (x1, x2)
